{
  "name": "package-test",
  "type": "module",
  "scripts": {
    "build:all": "npm run --workspaces --if-present build"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "5.1.6",
    "vite": "^5.2.9"
  }
}
